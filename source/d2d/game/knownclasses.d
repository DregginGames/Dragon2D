/**
  d2d.game.knownclasses is a helper so that everything gets linked into the engine
  */
module d2d.game.knownclasses;

/// all the basic classes
import d2d.core.base;
import d2d.core.event;
import d2d.core.dbg.eventdebug;

// resources
import d2d.core.resources.texture;
